name: Build

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker-compose up --abort-on-container-exit; echo "result" >> $GITHUB_STEP_SUMMARY